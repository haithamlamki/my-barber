# Polish Backlog

Non-blocking UI/UX polish items deferred from sprint work. Not bugs — these don't
affect correctness, money, security, or the booking guarantee. Pick up opportunistically.

## Open

- **Booking confirmation page vertical layout.** Content is pushed to the top of the
  viewport with a large empty area below it; the page needs vertical
  padding/centering so the confirmation card sits balanced in the viewport.
  File: `apps/web/app/[locale]/book/confirmation/[code]/page.tsx`. Verify in both
  ar (RTL) and en (LTR). Logged 2026-06-02.
